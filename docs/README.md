# Documentation Index

All project documents except the GitHub front-page `README.md` live here.

- `setup_guide.md` — shortest step-by-step setup index.
- `BUILD_WINDOWS.md` — Windows build instructions.
- `DEPENDENCY_SETUP.md` — required tools/dependencies and compile_deps placement.
- `PORTABLE_SERVER_SETUP.md` — standalone/portable server folder layout, MariaDB setup, configs, SQL imports, and startup order.

For dependency setup details, also see:

- `../compile_deps/README.md`
- `../compile_deps/DEPENDENCIES.md`

Maintainer-only development references are in `../Dev_referance/`.
